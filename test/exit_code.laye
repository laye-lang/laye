// R %layec% --print-ir %s

// * define void main() {
// +   %0 = i32 69
// +   call void @exit(%0)
// + }
export foreign noreturn main() {
    exit(69);
}

// * declare void exit(i32)
foreign callconv(cdecl) noreturn exit(i32 exit_code);
