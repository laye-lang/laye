// R %layec --ir %s

// * define global.0 = int8\[15\] "Hello, hunter!\\00"

// * define exported layecc main() -> int64 {
// + entry:
// +   %0 = alloca ptr
// +   store %0, ptr @global.0
// +   %2 = load ptr, %0
// +   %3 = call ccc int32 @puts(ptr %2)
// +   return int64 0
// + }
export foreign int main() {
    i8[*] message = "Hello, hunter!";
    puts(message);
    return 0;
}

// * declare ccc puts(ptr %0) -> int32
foreign callconv(cdecl) i32 puts(i8[*] str);
