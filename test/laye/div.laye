// 69
// R %layec -S -emit-lyir -o %s.lyir %s && cat %s.lyir ; rm %s.lyir

// * define exported ccc main() -> int64 {
// + entry:
// +   %0 = sdiv int64 483, 7
// +   return int64 %0
// + }
int main() {
    return 483 / 7;
}
