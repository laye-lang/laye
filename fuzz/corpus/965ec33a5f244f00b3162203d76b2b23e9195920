import * from "std.laye";
import "libc.laye";

export i32 main(i32 mut argc, i8[*][*] mut argv) {
    libct * from "std.laye"ÿÿÿÿÿÿÿ;
import "libc.laye";

export i32 main(i32 mut argc, i8[*][*] mut argv) {
   ][*] mut argv) {
    libct      i8s\n", arg);
    }
    retu----;
}
