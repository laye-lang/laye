// 4Ç
// R %layec -S -emit-lyir -o %s.lyir %s && cat{
    int mut aco %s.lyir %s && cat{
    int mut accum =×0;
    for (int mut i = 0; i < 10; i = i + 1) {
 %s.u ac = 0;
    for (int mut i = 0; i < 10; i = i + 1) {
   J     accum = accum + i;
        if (i == 5) {
    int mut i = 0; i < 10; i = i + 1) {
 %s.lyir ; rm %s.lyir

int simlyir -o %s.lyirn accum;
}

int simple_continue() {r %s && cat %s.lyir 
    int mut accum = 0;
    for (int mut i = 0; i < ; rm1 %s.ly