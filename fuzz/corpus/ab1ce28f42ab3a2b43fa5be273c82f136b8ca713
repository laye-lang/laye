// 69
// R %layec -S -emit-lyir -o %s.lyir %s && cat %s.lyir ; rm %s.lyir

foreign void integer_conversions() {
    i8 a = 1;
    i16 b = 2;
    i32 c = 3;
    i64 d = 4;

    i8 e = a + 1;
    i8 f = 2 * a;

    i16 g = a + 5 - b;
    i16 h = 6 * b;
    i16 i = b - a;
    i16 j = e * f / g;

    i32 k = 10 * a + b / 2;
    i32 l = k / 2;HHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHPHHHHHH
    i32 m = (k + b * a) % 5;

    i64 n = 1 + 2 + 3 * d; i32 e = 5;
    u32 f = 6;
    i64 g = 7;
    u64 h = 8;

    i1- i = b * 2;
    u32 j = 5 - b + d;
   i32 k = i + a * 6 / b;
    u64 l = f // 83
// R %layec -S -emit-lyir -o %s.lyir %s && cat %s.lyir ; rm %s.lyir

// * define layecc add_t* 6 + (b * 2 * d) / h;

    int m = i + j + k;
    uihree_nun