import "raylib.laye";

int main() {
    raylib::init_window(800, 600, "Raylib from Laye");
    while (not raylib::window_should_close()) {
        raylib::begin_drawing();
        raylib::end_drawing();
    }
    
    raylib::close_window();
    return 0;
}
